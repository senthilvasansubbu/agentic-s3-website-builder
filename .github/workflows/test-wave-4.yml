# Naming standard: <Category> | <Scope> | <Purpose>
name: Test | Wave 4 | Medium Security and Config Gate

on:
  workflow_dispatch:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened, ready_for_review]
    paths:
      - "app.py"
      - "config/**"
      - "api/**"
      - "services/**"
      - "tests/**"
      - "requirements.txt"
      - ".github/workflows/test-wave-4.yml"
  push:
    branches: [main]
    paths:
      - "app.py"
      - "config/**"
      - "api/**"
      - "services/**"
      - "tests/**"
      - "requirements.txt"
      - ".github/workflows/test-wave-4.yml"

permissions:
  contents: read

concurrency:
  group: wave-4-tests-${{ github.ref }}
  cancel-in-progress: true

jobs:
  wave4-medium-security-config-tests:
    name: wave4-medium-security-config-tests
    runs-on: ubuntu-latest

    env:
      JWT_SECRET: "github-actions-test-secret-do-not-use-in-prod"
      OPENAI_API_KEY: ""
      STRIPE_SECRET_KEY: ""
      STRIPE_WEBHOOK_SECRET: ""
      STORAGE_SECRETS_KEY: ""
      AWS_ACCESS_KEY_ID: ""
      AWS_SECRET_ACCESS_KEY: ""
      AWS_REGION: "us-east-1"
      S3_BUCKET_NAME: ""
      SQLITE_DB_PATH: ":memory:"
      VERBOSE_MODE: "false"

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Wave 4 test suite
        run: |
          pytest \
            tests/test_auth.py \
            tests/test_website_crud.py \
            tests/test_payment_webhook.py \
            --cov=services \
            --cov=api \
            --cov=config \
            --cov-report=xml:coverage-wave4.xml \
            -v \
            --tb=short \
            --maxfail=1

      - name: Upload Wave 4 coverage
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-wave4-xml
          path: coverage-wave4.xml
          retention-days: 30

  wave4-health-gate:
    name: wave4-health-gate
    needs: [wave4-medium-security-config-tests]
    uses: ./.github/workflows/health-gate.yml
    with:
      start_local_server: true
      base_url: http://127.0.0.1:8000
      require_auth_negative_401: true
      expected_health_status: "200"
