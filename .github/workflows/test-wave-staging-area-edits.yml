# Naming standard: <Category> | <Scope> | <Purpose>
name: Test | Wave Staging Area Edits | Staged HTML and Manifest Gate

on:
  workflow_dispatch:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened, ready_for_review]
    paths:
      - "app.py"
      - "api/**"
      - "frontend/**"
      - "services/**"
      - "tests/**"
      - "requirements.txt"
      - ".github/workflows/test-wave-staging-area-edits.yml"
  push:
    branches: [main]
    paths:
      - "app.py"
      - "api/**"
      - "frontend/**"
      - "services/**"
      - "tests/**"
      - "requirements.txt"
      - ".github/workflows/test-wave-staging-area-edits.yml"

permissions:
  contents: read

concurrency:
  group: wave-staging-area-edits-${{ github.ref }}
  cancel-in-progress: true

jobs:
  wave-staging-area-edits-tests:
    name: wave-staging-area-edits-tests
    runs-on: ubuntu-latest

    env:
      JWT_SECRET: "github-actions-test-secret-do-not-use-in-prod"
      OPENAI_API_KEY: ""
      STRIPE_SECRET_KEY: ""
      STRIPE_WEBHOOK_SECRET: ""
      STORAGE_SECRETS_KEY: ""
      AWS_ACCESS_KEY_ID: ""
      AWS_SECRET_ACCESS_KEY: ""
      AWS_REGION: "us-east-1"
      S3_BUCKET_NAME: ""
      SQLITE_DB_PATH: ":memory:"
      VERBOSE_MODE: "false"

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Wave Staging Area Edits tests
        run: |
          pytest \
            tests/test_wave_staging_area_edits.py \
            tests/test_staging_artifacts.py \
            --cov=api \
            --cov=frontend \
            --cov-report=xml:coverage-wave-staging-area-edits.xml \
            --junitxml=test-results-wave-staging-area-edits.xml \
            -v \
            --tb=short \
            --maxfail=1

      - name: Upload Wave Staging Area Edits coverage
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-wave-staging-area-edits-xml
          path: coverage-wave-staging-area-edits.xml
          retention-days: 30

      - name: Upload Wave Staging Area Edits test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: test-results-wave-staging-area-edits-xml
          path: test-results-wave-staging-area-edits.xml
          retention-days: 30
